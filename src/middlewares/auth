// middlewares
const adminauth = (req,res,next)=> {
    const token = 'xyzdsd';
    const isAuthorized = token === 'xyz';
    if(!isAuthorized) {
        res.status(401).send("Unauthorized access");
    }
    else {
        next();
    }
}
module.exports = {
    adminauth,
}